## Session 27 — Round 9 PICK (engine r404, in progress; 19 Sept ≈15:25 NZST): the alert-top side column's class follows the subject — `col-md-4 offset-md-0 col-12 paddingL` in NCEA1 / Mathematics / TMoA

- **The class (the position-free label census's MISSING side):** `div.col-12.col-md-4.offset-md-0.paddingL` — gold 392 / Claude 0 — and `div.col-12.col-md-4.offset-md-0` gold 682 / Claude 116. `_s27_r9_sidepair.py` (every gold `div.row` pairing a col-md-8 with a col-md-4 / col-md-3 on the paired pages, 2065 rows / 902 pages, by the right column's first child): the `alert top` side column (n = 160) is `col-12 col-md-4 offset-md-0 paddingL` 0.51 corpus-wide — a tie — but SOLID by subject: NCEA1 0.70 (33 / 47), Mathematics 0.75 (15 / 20), TMoA 0.86 (6 / 7); English 0.32 / Leaving to Learn 0.45 are ties; Fundamentals-English uses col-md-3 (n = 8). Claude's 56 alert-top side pairs (the r333 rule) all ship `col-md-4 col-12`. The `alertActivity` sidebar (n = 414) is `col-12 col-md-4 offset-md-0` 0.48 = Claude's own form (no change); the image sidebar (806) is the r331 / r3 declined class (images not in the WT).
- **The fix:** `callouts.positional_side_alert.after_content.side_column_by_subject {NCEA1, 1-10 Mathematics, TMoA → "col-md-4 offset-md-0 col-12 paddingL"}` + `side_column_by_subject_env SIDECOLSUBJ_OFF` — `#sideAlertCol` resolves the class by the module's `module_meta` subject; the ties keep the r333 class. OFF = today's output.
- **Gate expectation:** a one-line column-label change on the NCEA1 / Mathematics / TMoA alert-top pages (≈ 40 of the 56); small; **this ship is the FULL regeneration backstop** (the ledger at scoped #7 since r396).
