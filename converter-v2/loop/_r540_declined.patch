diff --git a/converter-v2/app/js/ContentConverter.js b/converter-v2/app/js/ContentConverter.js
index 02c0251..d706d56 100644
--- a/converter-v2/app/js/ContentConverter.js
+++ b/converter-v2/app/js/ContentConverter.js
@@ -3980,10 +3980,20 @@ class ContentConverter {
 							&& !(typeof process !== "undefined" && process.env && process.env[_as.env || "RHSALWAYS_OFF"])
 							&& (_as.template_types ?? []).map(String).includes(String(DataService.Data.ModuleStructureIndex?.module_meta?.[String(run?.moduleCode || "")]?.template_type ?? ""));
 						const _rhsKw = [...(psa?.keywords ?? []), ...(_asOn ? (_as.extra_keywords ?? []) : [])];
+						// ROUND 540 (session 53 Round 6; data positional_side_alert.side_words; env SIDEWORD_OFF): the writer's own
+						// "side" / "beside" names the side column too — `[Important side note]` (BLL110 / 120 / 130), `[alert side box]`,
+						// `[Alert box – beside the activity 2F above]` — in every template, never before "tab" (a side-tab).
+						const _sw = psa?.side_words;
+						const _swOn = !!_sw && _sw.enabled !== false
+							&& !(typeof process !== "undefined" && process.env && process.env[_sw.env || "SIDEWORD_OFF"]);
 						if (psaOn) {
 							const words = it.parse.tags.map((t) => t.remainder ?? "").join(" ")
 								.toLowerCase().split(/\s+/).filter(Boolean);
-							if (words.some((w) => _rhsKw.includes(w))) {
+							const _swords = _swOn ? it.parse.tags.map((t) => t.remainder ?? "").join(" ")
+								.toLowerCase().split(/[\s\-–—:,.]+/).filter(Boolean) : [];
+							const _sideWord = _swOn && _swords.some((w, wi) => (_sw.words ?? []).includes(w)
+								&& !(_sw.deny_next ?? []).includes(_swords[wi + 1] ?? ""));
+							if (words.some((w) => _rhsKw.includes(w)) || _sideWord) {
 								const spansAhead = this.#explicitCloseAhead(bodyItems, i, primary.tag)
 									|| this.#calloutWrapsStructured(it, bodyItems, i);
 								// an EMPTY box (no embedded lead, no black text of its own, no unconsumed
diff --git a/converter-v2/data/Emit_Templates.json b/converter-v2/data/Emit_Templates.json
index 5b45d80..1f8ed69 100644
--- a/converter-v2/data/Emit_Templates.json
+++ b/converter-v2/data/Emit_Templates.json
@@ -1059,6 +1059,13 @@
 			"env": "ALERTRHS_OFF",
 			"tags": ["alert", "important"],
 			"keywords": ["rhs", "rhc"],
+			"side_words": {
+				"enabled": true,
+				"env": "SIDEWORD_OFF",
+				"words": ["side", "beside"],
+				"deny_next": ["tab", "tabs"],
+				"_doc": "ROUND 540 (the autonomous loop, session 53 Round 6). The writer's own 'side' / 'beside' names the side column too — `[Important side note]` 18 (BLL110 / 120 / 130), `[alert side box]` / `[alert box side]` / `[Alert side]` 7, `[Alert box – beside the activity 2F above]` 2 — which the keyword list above never read, so the box shipped full-width with a 'side note' writer note where the gold builds the side column (BLL120 1.0: `col-md-4 … > div.alertActivity`; `outputs/_s53_r6_sidealert.py`: 110 gold side boxes Claude ships full-width). Chris's D15-18 ('an RHS tag always becomes the right-hand side box') — the vocabulary of a decided rule; every template (the base positional path). Never before 'tab' (`[Alert side tab …]` is a side-tab). Env SIDEWORD_OFF."
+			},
 			"always_side": {
 				"_doc": "ROUND 505 (the autonomous loop's session 49 Round 5 — Chris's D15-18, 25 Sept 2026, Option B: 'An RHS tag always becomes the right-hand side box'). In STANDARD and FUNDAMENTALS modules (not Inquiry — its golds 3 / 10) every right-hand alert spelling becomes the side column, the box style still chosen by position (r333: alertActivity after an activity box, alert top after content). Two widenings of r333: (1) extra_keywords — the writer's `[Alert box right hand side (of page)]` / `[right-hand alert]` spellings, whose words r333's rhs / rhc list never matched (BLL242 / 244 / 251 / 254–257 / 272 built full-width); (2) forward_pair — a box with NO content row closed just before it (the page-top Ākonga notes, the WJFUN tile pages, a box right after a heading) is HELD and paired as the right sibling of the FOLLOWING content row (the r123 pendingSideAlert machinery the alert-table path already uses) instead of a full-width box. Measured (outputs/_rhsalert_measure.py, repaired at r505): the gold side column 92 / 139 = 0.66 (Standard 0.70, Fundamentals 0.66). The gold's full-width / paragraph forms where they differ are NAMED overrides by Chris's decision. Env RHSALWAYS_OFF.",
 				"enabled": true,
