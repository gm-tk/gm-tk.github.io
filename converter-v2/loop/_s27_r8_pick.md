## Session 27 — Round 8 PICK (engine r403, in progress; 19 Sept ≈14:55 NZST): an `[embed]` of an external web page is the KB's externalButton, never a bare iframe

- **Measured first and DECLINED this round:** the r6 census's "211 synthetic boxes around a hand-off widget on Claude-more pages" — `_s27_r8_mergebox.py` over every synthetic (heading-less, single-widget) Claude box on the paired lesson pages (387): the gold ships a box with the SAME number on 241 = 0.62 (the r217 rule is right), merges the widget into the preceding box on 31 = 0.08, has no box for the previous number on 61 — no merge class.
- **The class (the position-free label census, `_s27_r3_labelcensus.out`):** `div.ratio.ratio-16x9` — the `[embed]` route's bare iframe wrapper — is a label the gold ships **0** times against Claude's 133. `_s27_r8_bareratio.py` (129 sites / 77 pages / 58 modules on the paired pages): the gold iframes NONE of them — absent 91 (worksheet / PDF pages the developer re-sourced or dropped: kindergartenworksheets 18, twinkl 11, tahurangi 10, sharepoint 17), an anchor 38 (24 buttons — csfieldguide / scratch / padlet / tuigarden / codeavengers / natlib / nzhistory — and 14 plain `<a>`, the BLL parents' worksheet links). Every URL the gold keeps is a LINK; by group Fundamentals / Technology button 0.77, LtL button 0.67.
- **The fix:** `elements.embeds.external_button {enabled, env EMBEDBTN_OFF}` — the `[embed]` route emits the r338 `buttons.external_destination` form (`<a href target=_blank><div class="externalButton">Go to website</div></a>`, KB 05D + constraint 75) for a URL whose host is not on the r338 internal-host list (Google Docs / Drive / SharePoint keep the iframe — the gold drops those 20 unknowably); a video host never reaches the branch. OFF = the bare iframe.
- **Gate expectation:** the 24 button sites gain the gold's `a > div.externalButton` lines, the 14 anchor sites their `a`; the 91 absent sites swap two EXTRA lines for two others (neutral); scored on the probe's ON pages before regenerating.
