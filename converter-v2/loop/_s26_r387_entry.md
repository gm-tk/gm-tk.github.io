## 2026-09-19 (round 387, build 260619.58) — THE WHAKATAUKĪ DOES NOT CLOSE ITS COLUMN: the writer's commentary paragraph after the proverb flows on inside the same `col-md-8` (`callouts.flow_after_tags`, env `WHFLOW_OFF`); the autonomous loop's session 26 Round 1 — found by a NEW instrument angle (the score-band ledger + the after-block census extended to every block type), not by the miner; SCOPED regeneration of the 59 whakataukī modules (scoped ship #7 since the r377 full — the next ship is the FULL backstop); skeleton 53.351 → 53.395 % (+0.043pp; 62 movers 54 up / 8 down, the dips named), ≥50 1151 → 1152, ≥75 185 → 189, ≥90 15, RAW 37.675 → 37.688 %; every other gate EXACT

### 1. WHAT CHANGED

**A boxed callout closes the section row after itself (the r51 `row_breaks.after` rule) — and for the whakataukī that is wrong.** The gold's `div.whakatauki` OPENS its column (FIRST-in-column 0.78 — the break BEFORE it is right, and stays) but is NOT its column's last child: the writer's commentary paragraph after the proverb (`[Whakatauki]` → the reo line + the English line → `[Body] Manaakitanga is when we show love…`) flows on INSIDE the same `col-md-8` on 0.70 of the Standard template's 81 boxes (57 pages / 51 modules). Claude closed the row after the box and opened a fresh `row > col-md-8` for the commentary — four extra skeleton lines on every such page (CEDW101 / AGH1002 / OSAI401 …).

**The fix (DATA OVER CODE):** `Emit_Templates.json` `callouts.flow_after_tags` `{enabled, env: "WHFLOW_OFF", tags: ["whakatauki"], templates: ["Standard"], exclude_subjects: ["NCEA1"]}`; `ContentConverter.#flowsAfter(tag, run)` reads it (the module's `Module_Structure_Index.module_meta` template_type / subject — an unknown module keeps the r51 break) and the two after-box `breakRow()` sites — the strict box (`if (!flowingCallout && !stack.length) breakRow()`) and the CONTAINER_CLOSE of a spanning box — skip the break for a listed tag in scope. The break BEFORE the box (`rowFor("section")`) is untouched. OFF = the r386 output on every page (the in-memory probe: 2109 / 2109 byte-identical).

### 2. HOW IT WAS FOUND (the session-26 instruments — the reason a class exists after four §4 exhaustion stops)

- **`outputs/_s26_ledger2.py`** — the s25 loss ledger re-cut by the gate's own SCORE BAND (≥ 0.90 / 0.75–0.90 / 0.60–0.75 / 0.40–0.60 / < 0.40), with the chrome family split by (direction · region · signature · modules) and a chrome-aligned test (`_s26_ledger2.out`). On the NEAR-MISS band (170 pages ≥ 0.75, loss 20.5pp) the largest converter-owned cell is wrapper-form EXTRA `div.col-12.col-md-8` 0.42pp + `div.row` 0.22pp — Claude opening a row the gold does not.
- **`_s26_extrarow.py`** — every Claude EXTRA row whose text all matched, on the pages ≥ 0.60 (284 rows), keyed by what CLOSED the previous row: a long tail (p 90 / h3 35 / WIDGET 32 / alert 28 / …) — no single closer over the floor, but it showed the s25 after-block census had measured only alert / activity / WIDGET.
- **`_s26_after2.py`** (the s25 `_s25_after.py` extended to every block type: whakataukī, button, table, img, video, iframe, audio, blockquote, the alert variants, list, p) + **`_s26_before2.py`** (the before-position twin): **the whakataukī is the ONE block whose gold after-rule contradicts r51** — gold followed-on 0.70 vs Claude 0.00 (76 / 76 LAST in column); every other type agrees with r51 or is a tie (alert 0.26 followed-on = LAST 0.74, alert.solid 0.90, alert.top 0.97, alertActivity 0.99, activity 1.00; img gold 0.40 / Claude 0.71 = the gold breaks after an image at 0.60, a tie; table 0.44 / 0.56; video 0.39 / 0.45; WIDGET 0.34 / 0.37; list 0.72 / 0.68; p 0.67 / 0.70).
- **`_s26_whpair.py`** — the PAIRED measurement (the session-25 lesson: a gold-side census must survive pairing). For each Claude whakataukī row, the next row's first text located on the gold page: **Standard FLOW 35 / BREAK 8 = 0.81** (35 pages / 35 modules); Online Safety 24 / 2 = 0.92; Leaving to Learn 4 / 1; ConnectED 3 / 0; Mathematics / English / Science 1 / 0 each; **NCEA1 1 / 3 = 0.25** (HIS1005 / 1006 / 1008 "The whakataukī chosen for this module…" opens a gold row; census 0.56 = a tie) → EXCLUDED; Inquiry 0 / 1, Bilingual (TMoA 5 / 5 LAST) → not listed; gold-nowhere 19 (the commentary the human dropped — untouched by the fix); untouched 10 (a heading / wananga / widget / alert follows — those open a row regardless).
- **KB-first check:** 05D "Whakatauki" gives the box form only (`div.whakatauki > p + p`), 01F the tag map; no KB rule on the row after it → authority LOOP §1b level 3 / 4 (the gold's own group consensus). Structure-only → derivable by construction.
- The session also decomposed the module-menu loss (3.62pp = chrome's largest cell) with `_s26_menucensus.py` / `_s26_menudeficit.py`: the 2,735 gold lesson-menu items Claude lacks are the r110 verbatim repeat 33 % (needs-Chris #4), editorial 23 %, gold-other-lesson 13 %, WT-derivable 9 % spread thin, TEDC402's off-by-one pairing (its Claude page 1 is a spurious "This module explores / Links to" page) — nothing new over the floor; recorded.

### 3. THE PROBE (in memory, one toggle state per process — `outputs/_s26_r387_probe.cjs` / `_s26_r387_probe_run.sh`, 4 shards over all 416 modules)

- **OFF (`WHFLOW_OFF=1`): identical 2109 / changed 0 / missing 0** — byte-identical to the r386 disk on every page.
- **ON: changed 63 pages / 59 modules** (`_s26_r387_changed_modules.txt`): CEDO301 CEDR501 CEDT501 ×2 CEDW501 ENGJ101 ENGJ102 ENGR102 ENGR202 EXBP901 ×3 EXIP901 ×2 HPRE203 MXDB201 MXEO202 MXEX302 OSAH301 / 401 / 501 OSAI101–501 OSBY101–501 OSGM101–501 OSOH101–501 OSSC301 / 401 / 501 OSSM301 / 401 SCCH301 SCPH301 SSCI205 XDLS902 / 904 / 905 / 906 / 911 / 912 XGF9001 XGF9004 XLP01 / 03 / 05 XMES201 / 203 XTAS101 — every one a Standard, non-NCEA1 module with a whakataukī the writer follows with prose; the other 357 modules (Inquiry / Bilingual / NCEA1 / no whakataukī / nothing follows) byte-identical under ON.
- **Scored with the gate's own `match()` BEFORE regenerating (`_s26_r387_pagescore.py` → `_s26_r387_onscore.json`): 62 paired pages, 54 up / 8 down / 0 same, pp-sum +84.7 (mean +1.37pp per changed page).** Best: ENGR202_0_0 68.3 → 72.5, OSOH201_0_0 97.1 → **100.0**, OSOH301 93.8 → 96.8, OSOH401 90.6 → 93.5, OSSC501 89.2 → 92.1, OSSC401 94.3 → 97.1, OSGM101 91.2 → 93.9, OSSC301 86.2 → 88.9. **The 8 dips are the paired probe's 8 gold-BREAK pages — the family's 0.19 minority, named:** OSAI201_0_0 −3.4 (its gold breaks after the proverb), SSCI205_0_0 −2.8, HPRE203_0_0 −2.6, EXBP901_2_0 −1.0, OSBY401_0_0 −0.8, CEDO301_0_0 −0.7, EXIP901_5_0 −0.6, CEDW501_1_0 −0.3.

### 4. THE SCOPED REGENERATION + THE GATES (scoped ship #7 since the r377 full — `_ship_ledger.py`: 1 of headroom; the NEXT ship is the FULL backstop)

- 59 modules in 6 batches (`_s26_r387_batches.sh` / `_s26_r387_regen_run.sh`, two at a time, every batch rc 0); `_content_manifest.py fresh --affected` → **0 truly stale: 59 affected regenerated, the untouched 354 byte-identical to the manifest** (`_s26_r387_fresh.log`); **probe ON == regenerated disk on 372 / 372 pages** (`_s26_r387_regen_vs_probe.log`).
- **PRIMARY skeleton (`_s26_r387_sk_final.json`, a fresh full run): SCAFFOLD 53.3513 → 53.3946 % (+0.043pp) / ≥50 1151 → 1152 / ≥75 185 → 189 / ≥90 15 / RAW 37.675 → 37.688 % @ 1956 pairs, skipped 0; 62 movers (54 up / 8 down, pp-sum +84.7 = the probe's prediction exactly), 0 added / dropped; = 58.1 % of achievable** (ceiling 91.9 %).
- compare_structure exact **11643** / EXTRA **172** / MISSING **625** EXACT (a row / col wrapper never enters the wrapper chain of the text-matched elements); body_compare **42 / 4 / 173 / 218** EXACT; clean **2079 / 2102**, leak **26 / 23** EXACT; tags **9557 / 9557 REAL 0**; flipCard 61 / divergence 0; speechBubble 62 built, at baseline (OSAI401 3, OSAH501 1); modal 38 / defect 0; mtkQuiz 17 / defect 0; math 323 / 323; menulabels 91 / 0; dragAndDrop 21 / defect 0; entry-parity PASS; index-sync OK; all 15 verifier selftests + the skeleton selftest GREEN (`_s26_r387_selftests.log`); fast-loop baseline, content manifest and feature index refreshed (`_s26_r387_postship.sh`).

### 5. RECORDED

- The 8 gold-BREAK pages above are the family's editorial minority (no discriminator between them and the 35 FLOW pages — same tag, same writer shape); NCEA1's HIS series is the one subject group where the break is the majority and is excluded by data.
- The gold's "followed by repeat 0.19" (an inner `div.row > div.col-12` nested in the same column after the box, CEDW101's form) is the r229-measured inner-row anatomy (gold same_col 75.8 %), unchanged by this round — those pages neither gain nor lose.
- Not taken from `_s26_after2.py` (all ties or r51-right): the image's after-break (gold 0.60 LAST vs Claude 0.29), the table's (0.56 / 0.44), the video's (0.61 / 0.55), the audio's (n = 43).
