## Session 27 — Round 10 PICK (engine r405, in progress; 19 Sept ≈15:20 NZST): the journal section is an activity box — a free heading whose section ends in a go-to-journal button is boxed in English / Leaving to Learn

- **The class (Round 6's `_s27_r6_journalsec.py`, every free heading section on Claude's paired pages holding a journal / activity-id button, the gold's rendering of that heading):** 115 sites; the gold BOXES the section (`gold:box`) in **1-10 English 15 / 17 = 0.88** and **Leaving to Learn 9 / 10 = 0.90**; NCEA1 0.50 / Mathematics 0.45 are ties; EXPlore's gold drops the heading (0.68 absent); ConnectED 0.45. The writer typed `[H3] Title` + prose + `[button] Go to journal` with no `[Activity]` opener; the gold's developer boxed the task (a numbered box: h3 title + prose + the goJournal h4).
- **The fix:** `opener_rule.id_heading_opener.journal_section {enabled, env JOURNALBOX_OFF, subjects, heading_tags, stop_tags, max_items}` — `InteractiveScanner.#idHeadingOpeners` (the r364 / r377 re-tagger) re-tags a free h2–h4 heading as a bare `[Activity]` opener when its section (the items before the next heading / opener / section marker / table / widget invocation) carries a `[button]` `#isGoJournalButton` recognises, in a listed subject; the heading's words are the box title, the r400 positional letter numbers the box, r239 puts the button's goJournal h4 inside. A heading directly after a writer's `[Activity]` opener is that box's title and is never re-tagged. OFF = today's output.
- **Gate expectation:** the English / LtL journal sections gain the gold's `div.activity[number] > row > col-12` chain around h3 + p + h4.goJournal; later boxes on the page re-letter (the r400 counter); scored on the probe's ON pages before regenerating. **This ship is the FULL regeneration backstop** (scoped #7 since r396).
