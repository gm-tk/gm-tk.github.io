## 2026-09-19 (round 399, build 260619.70) — THE WĀNANGA / TALANOA BOX IS THE KB'S CULTURAL ALERT: the CED Phase-5 writer's `[Wānanga/Talanoa box]` + one-cell table ships as `div.alert.cultural[layout=combined] > row > col-12 > p…` — not an empty `div.wananga` with a red flag and a kept `<table>` (the autonomous loop's session 27 Round 2; 47 pages / 6 modules; SCOPED regeneration of the 6, the probe proving the other 410 byte-identical; scoped ship #3 since the r396 full)

### 1. WHAT CHANGED

**The class.** The ConnectED Phase-5 writers (CEDK501 / CEDO501 / CEDO502 / CEDR501 / CEDT501 / CEDW501) type `[Wānanga/Talanoa box]` — 24 + 20 spellings, plus `[Wananga box]` ×4, `[Talanoa/Wananga box]` ×2, the typos `[Wanana/…]` / `[Wananaga/…]`, and CEDT501's `[Banner - Wānanga/Talanoa box]` ×11 — on its own line, then a ONE-ROW ONE-CELL table holding the discussion prompt (the cell's paragraphs " / "-joined; sometimes "• " bullets). The gold renders every one as the KB's Cultural Alert: `<div class="alert cultural" layout="combined"><div class="row"><div class="col-12"><p>…</p>…` — 63 / 63 gold boxes are `layout="combined"` (KB 14A §14.4 ConnectED Phase 5: *"all alerts are combined unless otherwise specified"*), a bulleted cell becomes a `<ul>`, no `<h4>` (0 / 63), and the tail-form `[Banner - …] prompt` becomes the same box with the tail as its `<p>`. Claude shipped an EMPTY `<div class="wananga">` carrying *"Red Flag: Empty [wananga] — the writer left a callout with no content"* (56 flags on 35 pages / 6 modules — the tag resolves to the `wananga` CONTAINER_OPEN, the lexicon's `wananga` / `talanoa` / `cultural alert` aliases with `[…box]` embedded, but the strict gather finds no black text before the table) + the writer's table as a kept `table.table-bordered`; the `[Banner - …]` form resolved to `carousel` (the `banner` alias outranks the container) and opened a rotateBanner bundle that declined into a hand-off box.

**The fix (DATA OVER CODE, one env `WANANGA_OFF`).** (1) `Emit_Templates.callouts.by_tag.wananga.kb_form {enabled, env, open '<div class="alert cultural" layout="combined">', close, wrap_content: true}` — `ContentConverter.#calloutOpen` swaps the def's open / close / wrap when on (the legacy `<div class="wananga">` stays as the OFF form). (2) `callouts.by_tag.wananga.table_cell_content {enabled, env}` — in STRICT mode a callout whose own gather is empty and whose next unconsumed item is a one-row one-cell table takes that cell as its content through `TablesAndGrids.renderCellParts` (a `<p>` per " / "-part, "• " lines grouped into a `<ul>`, the cell's own `[H*]` / `[image]` parts honoured), marks the table `_consumed` (the emit site's `while _consumed` skip steps over it); the new `#calloutTableCell` helper is also consulted first by `#calloutWrapsStructured`, so the box never span-wraps a kept table instead. (3) `Tag_Lexicon._meta.tag_promote` rule `{from: carousel, to: wananga, to_directive: CONTAINER_OPEN, clear_remainder: true, when_match: '^\s*\[\s*banner\s*[-–—:]\s*w[āa]nanga', env: WANANGA_OFF}` — CEDT501's `[Banner - Wānanga/Talanoa box]` is the same box (its 10 gold pages); `[Rolling Banner - …]` (1 site, a 4-cell strip the gold dropped) is excluded by the anchor; `clear_remainder` stops the promoted tag's leftover "/ box" surfacing as a spurious Writers Note. Config `260619.70`.

### 2. HOW IT WAS FOUND

The paired callout-class census (`outputs/_s27_r2_alertclass.py` — every gold callout box paired to the Claude box holding the same opening words, gold class-set × Claude class-set by template / subject): the gold-only `alert cultural[layout=combined]` 58 boxes with NO Claude counterpart. The writer's tag traced by `_s27_r2_wananga.py` (69 tag sites / 7 modules; per site the text → the gold page's wrapper → Claude's wrapper: gold `alert cultural` 51 of 53 found, Claude `table` 58 / cv2 box 3 / carousel 1) and generalised by `_s27_r2_callouttbl.py` over EVERY callout tag typed bare + a one-cell table on all 416 WTs: the idiom is exactly this class — `[wananga…]` 55 sites / 6 modules / 37 gold pages, gold `alert cultural` 50 = **0.91**, `alert` 1 (CEDK501's overview explanatory box), 2 inside a gold accordion panel, 2 not found; the only other carrier is `[alert]` on TEDC402's page-layout tables (7 sites, gold not found) — so the rule is opt-in per def, not a general callout rule. The row-break after the box measured on the gold's 74 cultural boxes: LAST-in-column 59 / a sibling flows on 15 = 0.80 break → the r51 default stands (no `flow_after_tags` entry); inside an activity box 2 / 74 (the standing "a container opener closes the open activity" class, not this round's).

### 3. AUTHORITY (LOOP §1b)

KB 05B "Cultural Alert (Wānanga / Talanoa)" — the exact form, `layout` ∈ {wananga, talanoa, combined}; KB 14A §14.4 ConnectED Phase 5 — the `alert cultural layout="combined"` snippet + *"all alerts are combined unless otherwise specified"*; KB 01D tag taxonomy `alert-wananga` / `alert-talanoa` / `alert-combined` → `alert_cultural_*`; KB 01F the emit forms. The gold agrees at level 2 (the CED5 series 0.91, every module ≥ 0.85); no sibling contradicts (no other family uses the tag).

### 4. PROOF

- **The in-memory probe over all 416 modules** (`_s27_r399_probe.cjs` + `_s27_r399_probe_run.sh`, 4 shards): OFF (`WANANGA_OFF=1`) **2109 / 2109 byte-identical to disk**; ON **47 pages / 6 modules changed, 0 outside** (CEDK501 7, CEDO501 7, CEDO502 8, CEDR501 6, CEDT501 13, CEDW501 6; every diff the box: the empty `div.wananga` + its flag + the kept table → `alert cultural > row > col-12 > p / ul`; the rotateBanner family untouched — 410 modules identical).
- **Scored with the skeleton gate's own `match()` on the probe's ON pages BEFORE regenerating** (`_s27_r399_pagescore.py`): 47 changed paired pages — **39 up / 5 down / 3 same, pp-sum +200.8**; the five dips named: CEDT501_7_0 −4.4 (the gold flows the `[body]` after the box inside the same column — one of the 15 / 74 flow minority), CEDT501_2_0 −1.9 (activity 2A: the gold keeps the box INSIDE the box the `[Activity 2A]` opened — one of the 2 / 74 — while the CONTAINER_OPEN closes the activity first, the standing class; the activity also loses `interactive` as its typing widget now sits after the box), CEDT501_5_0 −1.4, CEDW501_2_1 −0.5 (its boxes sit inside a gold accordion panel), CEDT501_6_0 −0.3.
- **SCOPED regeneration of the 6** (`_s27_r399_batches.sh`, rc 0); `_content_manifest.py fresh --affected` **0 truly stale**, the 407 untouched byte-identical; probe ON == regenerated disk **79 / 79**.

### 5. PROTECTED GATES (all HELD-or-IMPROVED)

- **Skeleton (PRIMARY):** SCAFFOLD **53.759 → 53.862 % (+0.103pp)**, ≥50 **1166 → 1170** (CEDO501_2_2, CEDO502_5_0, CEDO502_7_0, CEDW501_5_0 cross up), ≥75 **195 → 197** (CEDW501_0_0, CEDO502_0_0), ≥90 **18**, RAW **37.846 → 37.918 %**, 1956 pairs / 0 skipped; **44 movers, 39 up / 5 down, 0 outside the affected set** (CEDO502_7_0 +24.2, CEDO502_0_0 +18.3, CEDW501_5_0 +14.2, CEDO502_5_0 +12.7, CEDW501_0_0 +10.3, CEDT501_0_0 +9.8; the dips as named above).
- **compare_structure:** exact **11723 → 11798 (+75 IMPROVED)** (the box's `<p>`s text-match the gold's), EXTRA **172** EXACT, MISSING **626** EXACT, row-wrap 23 EXACT (text-matched pool 13741 → 13816).
- **body_compare** 42 / 4 / 173 / 218 EXACT; **structural-defect audit** clean 2079 / 2102, leak 26 / 23 EXACT; **tags** 9557 / 9557 REAL 0; entry-parity PASS; index-sync OK; **every verifier RESULT line identical to r398** (flipCard 61 / divergence 0, speechBubble at baseline, accordion / tabs / clickDrop / dropdown / modal / mtkquiz / hintslider / carousel / math 323 / menulabels 91 / dragdrop 21 — defect 0); **15 selftests + the feature-index selftest GREEN**; fast-loop snapshot, content manifest, feature index refreshed; ledger scoped #3 since the r396 FULL.

### 6. RECORDED, NOT TAKEN

- The 2 / 74 gold boxes INSIDE an activity box (CEDT501 2A + one more): the `container_auto_close.activity_close_before.directives` CONTAINER_OPEN rule closes the open activity before any callout — a wider class (every callout typed right after an `[Activity]` opener), its own measured round.
- The 15 / 74 gold boxes a sibling flows after inside the same column: below the r51 break rule's 0.80 majority — not listed in `flow_after_tags`.
- CEDT501 L1489 `[Wānanga is a way of learning…]` — a writer's prose in a bracket; it resolves to `wananga` and ships the box (as it shipped `div.wananga` before) where the gold renders plain prose: 1 site, unchanged class.
- `[Rolling Banner - Wānanga/Talanoa box]` + a 4-cell table (CEDT501 lesson 2): the gold dropped the strip; left as the carousel invocation it is.
- The bare-`[alert]` + one-cell table idiom (TEDC402 ×7, the page-layout tables): not this rule's population.

