## Session 27 — Round 11 PICK (engine r406, in progress; 19 Sept ≈15:50 NZST): the bracket-less red `Activity 4A` line is the `[Activity 4A]` opener

- **The class (Round 10's recorded follow-up, `_s27_r11_bareact.out`):** a writer colours the activity opener red but types no brackets — `🔴Activity 4A🔴` then `[H4] Check your understanding` + a red instruction + the answer table (ENGI102 lesson 8; the gold ships box 4A with the built dragAndDrop) — so the span parses as noise (no primary) and the box never opens. Sized on the parsed WTs: 36 sites / 14 modules; 27 sites on the 8 tracked modules (ENGS302 7, PES1001 7, ENGI102 6, TWHA902 3, ANZH101 / ENGJ402 / SSFUN06 / TWHK902 1); the gold's box count exceeds Claude's on every one (ENGI102 16 vs 9, PES1001 20 vs 9, TWHA902 27 vs 1). The r148 bare-lead class (`Lesson overview:` typed without brackets) applied to the activity opener: a whole-span exact predicate cannot over-fire on prose.
- **The fix:** `opener_rule.bare_red_opener {enabled, env BAREACT_OFF, pattern}` — `InteractiveScanner.#bareRedOpeners` (a pre-pass beside `#idHeadingOpeners`) re-parses a tag-less red span whose entire folded text is `activity <id>` (optional bold markers / colon) as the typed `[Activity <id>]` opener; every downstream rule then sees a typed opener. OFF = the r405 output.
- **Gate expectation:** the missing boxes open on the ENGI102 / PES1001 / ENGS302 / TWHA902 pages (≈ 20 pages); scored on the probe's ON pages before regenerating; a SCOPED regeneration (the ledger's counter was reset to 0 by the r405 full).
