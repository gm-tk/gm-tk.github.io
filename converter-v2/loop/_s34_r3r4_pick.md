## Session 34 — Round 3 PICK pass (no engine change; 22 Sept ≈15:10 → 15:28) — every §4 lane on the r428 corpus, nothing at the floor except one Inquiry-family dialect
- **The miner's queue (re-mined 15:03 on r428, 196 CANDIDATE):** the diff against the r427 queue — NEW structural rows are the r428 re-alignments only (`body EXTRA div.inquiryPanel.showing › div.row` 27 / 25, `div.inquiryPanel › div.row` 46 / 45 — Claude's panels hold more `div.row`s than the gold's: the s25 order / wrapper-form residue, no discriminator); GONE: `root body.inquiry`, the col-md-8 substitution. **#4196 `body SUBSTITUTED col-md-8 › p vs h3`** (24 / 23, c = 0.84) read: the aligned lines carry DIFFERENT text ("The Amazing Cow" h3 vs a "Sheep, cattle" p) — a difflib alignment artefact, not a class. **The chip facts F7 `MISSING decimal` 81 / 33 (NCEA1 c = 0.81 n = 10) and F15 `EXTRA lesson-number` 45 / 16:** the pages are ART1004 / 1005 (an overview paired to a lesson page — the dual-build pairing artefact), BLL171–174 / 240 / 246 / 271 (the BLL17x / 24x mixed sub-series r427 left on purpose), ANZH101 / 103 / 302 / 401 / 404 (a family mixed BY MODULE); the NCEA1 gold chip census by prefix (AGH decimal 72 / ART 18 / CBI 30 / … / HIS decimal 56 vs padded 21 / MXS padded 9 / GER mixed) has no uniform family the registry does not already say → no candidate; F7 / F15 recorded as scatter.
- **The dashboard / widget lane (D10-3):** the r271 variation census + r286 decline recorder rebuilt on the r428 corpus (`_s34_r3_dashboard_run.sh`, 16 shards ×2, 15:16 → 15:19): interactive coverage **50.7 %** (2902 / 5719); un-built dragAndDrop 919 / 771 shapes, carousel 320, accordion 293, flipCard 260, clickDrop 343, selfCheck 137, modal 170, tabs 113, slider 52, infoTrigger 43; the 50 intake modules' declines by shape (`_r271_variations.json`): the largest single shapes are 11 (accordion `noTable || accordion+txt`), 10 (carousel `blk*2`), 6, 6, 6 — nothing ≥ 20 sites; the cross-cutting blocker `dragAndDrop (a captured TABLE)` 651 declines is the quiz-engine / table question (Needs Chris #4).
- **The loss ledger re-run on r428 (`_s34_r3_ledger.out`):** total loss 46.94pp (s25: 47.94) — wrapper-empty 7.04, chrome 5.50 (module-menu `li` 0.75 = the lesson-menu content, Needs Chris #4), alignment-residue 5.10, editorial 7.54 (the ceiling), widget-line 2.10; by template Inquiry 59.73 (117 pages), Fundamentals 52.40, **Bilingual 63.68 (81 pages: container-shift free ↔ activity 7.0pp, editorial 17.2pp)** — TRR109 triangulated: the gold's TRR pages are `radioQuiz` / audio-image rows / bilingual `<p eng>` pairs the human builds from the writer's audio lists — the quiz-engine types (Needs Chris #4) and the human's media layout, not a rule.
- **THE IMAGE ROW (`_s34_r3_imgrow.py` / `.log`):** the gold's `div.row` of ≥ 2 image-only columns — 1171 rows on 484 pages (col-4 ×3 182, col ×3 45, col-md-4 ×3 82, col-md-6 ×2 103, col-1 ×N 31 …); on the paired Claude page the same images are CONSECUTIVE in one column on only **8** pages, scattered 222, inside a widget 29, not on the page at all 912 (widget internals — the tile rows — or developer-sourced images) → DECLINED (under the floor as a free-body class; A1 / C otherwise).
- **The KB queue §D:** no NOT CAPTURED row ≥ 20 pages (c67 27 gold pages DECLINED s3; c95 12 pages; c85 5 modules). **Recognition / no-build:** the 7 gold-only dirs = the 7 no-source modules. **Follow-up candidates:** the two r428 items — CEDO402's `[Side Tabs]`-widget shell (1 module) and the r100 mode's opener robustness (5 pages) — the second is ONE Inquiry-template dialect under §1d exception 1 → **taken as Round 4 (r429)**; #4235 declined (Round 2).

## Session 34 — Round 4 (engine r429, IN FLIGHT from 15:30 NZST 22 Sept) — THE r100 INQUIRY MODE'S OPENER ROBUSTNESS: a `[Tabs]` widget's consumed members never fire the mode, and an EMPTY lead segment folds into the first panel

**PICK (§1d exception 1 — a family dialect: the Inquiry template's own r100 mode on the 5 PANELS-DIFF pages the r428 census left — CEDK102 7 vs 6, CEDR204 7 vs 6, TWHR907 2 vs 4, TWHK907 2 vs 5, TWHA902 5 vs 9; authority §1b level 1, KB 06 lines 169 / 171 — the same rule as r428, scoped to the Inquiry template).** Two defects behind them (`_s34_r2_items.cjs` dumps): **(a)** the r100 detection counts EVERY `tab n` item — a `[Tabs]` WIDGET's consumed `[Tab]` members (TWHR907 items 115 / 123 / 151, TWHK907's) count as "empty openers", so the mode fires on a page whose real openers are labelled `[New side tab] Values` / `[New tab] 2` items with no digit; those are then "captured" as labels with no index (`inquiryLabels[n]` needs a digit) and open nothing — 2 panels with an empty crumb where the gold has 4 / 5. Counting only UNCONSUMED tab items leaves the mode un-fired there, and the r428 fallback takes the page (its `tab_opener_patterns` accept `[New side tab]` / `[New tab]`; a digit-only tail is not a label — the heading decides). **(b)** the r100 default build always prepends a synthetic `Introduction` intro panel (`crumb_intro`, `rel="intro"`) — on CEDK102 / CEDR204 / TWHA902 the writer's FIRST opener is the introduction (`[TAB 1] Introduction` in the list, an empty `[TAB 1]` at the very start), so the lead segment is EMPTY and the gold has N panels, Claude N + 1 with an empty first panel. When the lead segment holds no text and no media, the first opener's panel takes crumb 1 (the list's own label, else `intro_label`) — the r428 `hasIntro` rule applied to the r100 build. Data `inquiry_tabs.openers_unconsumed_only` (env `INQCONSUMED_OFF`) and `inquiry_tabs.empty_intro_folds` (env `INQEMPTYINTRO_OFF`); files `ContentConverter.js` (the three r100 counts), `PanelsBuilder.js` (the default build), `Emit_Templates.json`. Expected: exactly the 5 pages change (the OFF probe byte-identical elsewhere); the PICK predicts a skeleton move ≥ 0.02pp (TWHR907 / TWHK907 gain their panels).

