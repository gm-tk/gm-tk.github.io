- **ROUND 526 IN FLIGHT — NOT PROVEN** (session 51 Round 7, picked 26 Sept 13:08 NZST after a PICK pass — the activity-sidebar followers (`_s51_r7_sidegold.py`: 18 gold-in-activity / 12 free — under the floor), the XGF family (XGF9004's WT yields 18 pages to the gold's 7 — a single-module source oddity), then the loss ledger's largest family, BLL (298 pages, 10.2 % of the gap): the scoped miner (`_s51_bll_miner.md`) row #1374 `div.row` › gold `div.col-12` vs Claude `div.col-12.col-md-8` (107 pages / 78 modules) = the overview's `[Introduction]` heading: `_s51_r7_intro.py` — the gold gives it its own FULL-WIDTH row (`row > col-12 > h3 Introduction` alone) on 65 of 79 BLL overviews found (0.82); Claude opens the ordinary `col-md-8 col-12` content row with it (71 / 71). Authority §1b-3 (the family's own gold; no KB rule). **THE BLL INTRODUCTION HEADING'S OWN FULL-WIDTH ROW** — a page post-pass (ContentConverter, beside #journalInstructionBox) keyed by subject ("1-10 Blended Literacy"): a row that OPENS with `<h3>Introduction</h3>` is split — the heading alone in `row > col-12`, the rest in the row's own column. Files: `app/js/ContentConverter.js`, `data/Emit_Templates.json` (`body_region.intro_heading_full_row`). Env `INTROROW_OFF`.
