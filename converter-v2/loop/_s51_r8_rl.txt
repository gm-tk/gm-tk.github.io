- s51-r8 (no engine change, 26 Sept ≈13:31 → 13:29) · a PICK pass — the BLL scoped miner's next rows (#105 `activity interactive` = developer-built widgets from plain writer content, e.g. BLL111 1C's randomSelect — class C; #103 / #1373 extra p / row = the writer's activities the human dropped — class C), scoped miners over ANZH / HIS / ENGI / PES (`_s51_{ANZH,HIS,ENGI,PES}_miner.md`: HIS's journal link vs Claude's `h4.goJournal` = D13-5's house form; PES's iframe class = the KB form; HIS `alert solid` 21 pages / 6 modules at 0.64 where the writer typed `[Alert]` — the writer's tag decides, KB c14), the lost-text probe (`_s51_r8_lost.py`: its 0.7 word-overlap match counts developer rewordings as 'lost' — not trustworthy) → no class at the floor · plateau 0 of 3 (neither).
