## Session 27 — Round 12 PICK (engine r407, in progress; 19 Sept ≈16:25 NZST; the budget's last round): the heading-led numbered opener takes the OWNER form — no empty box before its own section

- **The class (Round 11's recorded follow-up; `_s27_r12_emptyowner.py` on the r406 corpus):** a writer's numbered `[Activity N]` opener directly followed by an `[H2]`–`[H5]` heading (then prose, a table / widget) keeps the r362 MEMBER form: the heading terminates the member walk at once, so the bundle holds only the opener — the page ships an EMPTY box (the `no content captured` flag alone) with the heading / prose / table free AFTER it. Counted on the paired pages: **75 writer-owned empty boxes on 53 pages / 31 modules** (unclassified 52, dragAndDrop 14, multiChoiceQuiz 6, other 3); the gold's same-numbered box holds the section INSIDE — **h3 + widget 43, h3 + prose 13, h3 + table 3, widget 2 = 61 / 75 = 0.81**; absent 14. r363 kept the h2–h5-led member form on the r362 gate reading (MXFUN01 / MXFL203 / AGH1008); the gold on MXFL203's three is h3 + widget / widget / prose.
- **The fix:** `unclassified_activity_lead.heading_led_owner.empty_walk_owner {enabled, env HEADLEDOWNER_OFF}` — when on, a member-form heading no longer keeps the member form: every heading-led numbered opener takes the r362 OWNER form (opener → `activityOwner`, the heading + prose → `activityLeadItems`, the members from the first table). Scoped to the unclassified path (the 52); the typed-widget empty boxes (`[Activity 2A drag and drop]` + a heading, 20) are the normal path's and stay a follow-up. OFF = the r406 output.
- **Gate expectation:** the empty boxes fill with their h3 + prose (+ the table as the hand-off box) on ≈ 40 pages; scored on the probe's ON pages before regenerating; a SCOPED regeneration (ledger scoped #2). If the gate's own `match()` reads it net-negative (the r362 verdict re-found), DECLINE — ship inert (`enabled: false`), record the measurement.
