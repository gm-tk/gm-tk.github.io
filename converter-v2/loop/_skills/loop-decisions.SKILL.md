---
description: Explain every pending PageForge loop issue that needs Chris's decision, in plain English with real module examples, presented as the interactive EXPLAINER__Pending_Decisions_<date>.html page in the 23 Sept 2026 format (Chris's standing instruction of 25 Sept 2026), then record his answers so /loop-start can action them. NOT a loop run. Chris types /loop-decisions in a new session.
disable-model-invocation: true
---
This session explains the PENDING DECISIONS of the PageForge autonomous loop to Chris and records his answers. It is NOT a loop run: do NOT start a round, do NOT change converter code or data, and do NOT regenerate any module — this message carries no REGENERATE CORPUS code. Chris is present and is not a developer: every explanation must be in plain, everyday English, with every technical term defined the first time it is used, and every choice framed as what the choice is, what happens either way, and your recommendation — never a bare list of options.

1. GATHER, by section not whole (the §6 context diet): LOOP_STATE.md — FIRST its "## Needs Chris — open decisions" section (the standing one-line list, oldest first, kept by every session and every /loop-review since 22 Sept 2026), then the Blocked classes, the Declined classes that carry a "needs Chris" or "KB conflict" note, the Decisions from Chris already recorded (so nothing already answered is re-asked), and the latest "Next session starts with" line; KB_AMALGAMATION_STATUS.md rows marked as needing a decision or in conflict; any BLOCKED item in the recent BUILD_CHANGELOG.md entries. Build one de-duplicated list of open decisions, oldest first.

2. FOR EACH open decision, produce a section with exactly these parts:
   - A plain-English title and a one-paragraph explanation of what the issue is and why the loop could not settle it on its own.
   - THE REAL EXAMPLES — this is mandatory: pick 2–3 modules that carry the issue and for each show the three things side by side, quoted verbatim from the files (use grep/sed with line ranges, never whole files): what the writer typed in the Writers Template (the `_parsed.txt` region), what the human developer built (the gold HTML lines, from 01-Finalized_Modules_), and what PageForge currently produces (the lines from 01-Claude_Modules_). Give the file paths. Describe in one sentence what a learner would SEE in each version.
   - The numbers: how many pages / modules are affected, what the knowledge base says (quote the KB rule and its file), what the human gold does (the measured share), and what the loop's authority order (LOOP__Autonomous_Rounds.md §1b) would say if applied.
   - The choice, written as: Option A — what it is, what changes on the pages, what it costs or risks; Option B — the same; (Option C where one exists). Then: "My recommendation is … because …".
   - Where an option would change many pages, say whether it is gate-neutral or gate-moving and whether it needs a full regeneration.

3. WRITE the whole report to DECISIONS__Pending_<YYYY-MM-DD>.md at the folder root (the evidence record), THEN BUILD THE VISUAL VERSION — the deliverable Chris reads, by his STANDING INSTRUCTION of 25 September 2026 (D15-S1 in LOOP_STATE.md; LOOP §5b): `EXPLAINER__Pending_Decisions_<YYYY-MM-DD>.html` at the folder root, in EXACTLY the format of `EXPLAINER__Pending_Decisions_2026-09-23.html`. Build it by COPYING that file — it is the template: keep its CSS, layout, section order (sticky top bar · hero with stat tiles · "How to read" · "Corrections to the loop's own notes" · the decisions: tile grid + decision panel · the answer sheet · glossary · footer), element ids, classes and script UNCHANGED, never a different design — and replace only the content: (a) the `D` array in its `<script>`, one object per open decision, oldest first, in the template's exact shape — `n` the Needs-Chris number, `t` the plain-English title, `s` the size, `d` the date raised, `short` the tile label, `big:true` on the largest item, `why` one paragraph on why the loop could not settle it alone, `ex` the three columns `wt` / `gd` / `pf` each with `f` the real file and line range, `h` a simplified sketch of that spot built from the template's mini classes (`.doc` / `.tag` / `.pg` / `.m-tbl` / `.m-btn` / `.m-note` / `.m-menu` / `.m-box` / `.m-bub` / `.m-hand` / `.m-files`) and `s` the one sentence a learner would see, `note` the other examples, `o` the options A / B (/ C) each with `k`, `t`, `d` what changes and `c` the chips (`n` score unchanged · `m` score moves · `r` rebuild · `f` warning), `rec` (+ `recPlus` / `recAlt`) and `rw` the recommendation and why, `q` the one-sentence question, `tech` the under-the-hood list (the numbers, the KB rule with file and line, the §1b authority order); (b) the top-bar badge count; (c) the hero — date, headline, intro, the four stat tiles (open decisions, the biggest item's size, today's match score, the live answered counter); (d) the "Corrections to the loop's own notes" cards (what preparing the report found wrong in LOOP_STATE.md; remove the section if nothing); (e) the answer-sheet header line in the script (`My answers to the pending loop decisions (<date> report):`) and its "🎯 If you only answer one" verdict; (f) the glossary (add any new term); (g) the footer (source files, session date, the last shipped engine build). Every decision must be reachable from the tile grid, every option clickable, the answer sheet must fill in and copy. Open the page once in the built-in browser to check it renders (no console errors, every tile opens, Copy works). Then in the chat give a short summary, the HTML file's full path (the thing Chris opens), and the NUMBERED list of the questions, one per decision, each answerable in a sentence.

4. WHEN CHRIS ANSWERS (in this session, in any order, possibly partially — by pasting the page's answer sheet, or in his own words): for each answer, record it immediately under "## Decisions from Chris" in LOOP_STATE.md — date, the question in one line, the answer in his words, and exactly what it authorises the loop to do (the class, the scope, the target form, the authority it overrides). Update the matching Blocked / Declined entry to point at the decision. Commit LOOP_STATE.md in pageforge-site with the message "Decisions from Chris <date>". Never push. Then tell Chris which decisions are now recorded, which remain open, and that /loop-start will action the recorded ones in its next session.
