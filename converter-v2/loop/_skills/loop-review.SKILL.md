---
description: Periodic health review of the PageForge autonomous loop itself (run on Fable, NOT the loop) — audits the knowledge base, recent rounds, the loop rules, the intake state and the open decisions, then APPLIES every change it recommends to LOOP__Autonomous_Rounds.md, the SKILL.md copies and the next starting message (Chris, 22 Sept 2026: no approval gate). Chris types /loop-review in a new session.
disable-model-invocation: true
---
This session is a REVIEW of the PageForge autonomous loop, not a run of it. Do NOT start a round, do NOT change converter code or data, and do NOT regenerate any module — this message carries no REGENERATE CORPUS code. Chris may or may not be present; explain everything in plain English (define every technical term the first time you use it, no unexplained jargon). STANDING INSTRUCTION FROM CHRIS (22 September 2026): carry out every recommendation this review arrives at AUTOMATICALLY — make any and all changes to the loop instructions (LOOP__Autonomous_Rounds.md, its §7 message, the loop-start / loop-stop / loop-decisions / loop-review SKILL.md copies, .claude/settings.json hooks, KB_AMALGAMATION_STATUS.md housekeeping, the loop mirror) and amend the next starting loop (the loop-start message and LOOP_STATE.md's "Next session starts with" line) so that every future loop carries the updated instructional framework and keeps operating as autonomously as possible. Ask Chris ONLY when a recommendation would change converter code or data, regenerate modules, delete or reverse a recorded decision, or push.

1. HEALTH CHECK. `git status` and `git --no-pager log --oneline -30` in pageforge-site; `bash _MIGRATION/verify_after_transfer.sh`; delete any stale git lock; confirm the five files `.claude/settings.json`, `.claude/skills/loop-start/SKILL.md`, `.claude/skills/loop-stop/SKILL.md`, `.claude/skills/loop-review/SKILL.md` and `LOOP__Autonomous_Rounds.md` §7 agree with each other (the SKILL.md bodies are the copies that actually run; the loop-start body must equal the §7 message byte-for-byte apart from the `$ARGUMENTS` placeholder, and must carry NO state — no census, no round number); confirm the loop file's "Amended:" header line is present and its dated entries match the reviews that exist (a missing line means an older copy overwrote it — recover the text from the mirror's git history with `git show <hash>:converter-v2/loop/LOOP__Autonomous_Rounds.md`); `cmp` every file in `pageforge-site/converter-v2/loop/` against its live counterpart (`gate_baseline.json` lives in `CONVERTER_V2/reference/tests/`, which is not in git — the mirror is its only committed copy); count the `*.bak` files at the folder root (they belong in `_Backups/loop_state/`, LOOP §5d — move any found) and check the mirror's instrument snapshots (`_round_close.sh`, `_placement_census.{md,json}`, `_coverage_dashboard.json`) as well as the loop files. INTAKE CHECK (LOOP §1f): count the gold module dirs under `01-Finalized_Modules_/*/` and the docx files, compare with the verify script's census and with `Module_Structure_Index.json`'s `module_meta`, and list any gold module with no Claude dir that is not on the recorded no-build list — if any exist, the intake round (Round 0d) has not been run: say so in the report and make it the next session's Round 1. Report anything uncommitted, any stale lock, any drift.

2. READ, by section not whole (the §6 context diet): LOOP__Autonomous_Rounds.md (all of it — it is small); LOOP_STATE.md (the STOPPED entries since the last review, the Round log, Declined classes, Blocked classes, the "Needs Chris" list, Decisions from Chris, every "Loop review" entry, and the latest "Next session starts with" line); the archived round-log lines for the sessions since the last review (`grep -n '^## ' LOOP_STATE_ARCHIVE.md`, then the range); KB_AMALGAMATION_STATUS.md (header, §D queue and any rows changed since the last review); the headings of CONVERTER_V2/BUILD_CHANGELOG.md since the last review and the top entry in full; the knowledge base at 00-Other-TK-Resources/htmlconvertor-kb — `git log` there since the last review, the files it touched under 00_MASTER_INSTRUCTIONS / 14_SUBJECT_GLOBAL_PARAMETERS / 06 / 10, and the latest 12_CHANGE_LEDGER parts plus 12G, for any CL number newer than the highest one KB_AMALGAMATION_STATUS.md knows about; the latest LOOP_INTAKE__*.md §7 and §9; the newest gate log in CONVERTER_V2/outputs; the miner's DIFF_QUEUE.md header. Use parallel readers where the reading is independent, and keep every reader read-only and off native python.

3. ASSESS, with numbers, each of these — and for each say "fine as is" or "change needed" with the evidence:
   a. KB drift: new CL decisions, constraint changes or 14_ family changes not yet in KB_AMALGAMATION_STATUS.md; new conflicts between the KB and rules the loop has already shipped.
   b. Progress: the score as "% of achievable" over the rounds since the last review (separate what the converter earned from instrument corrections and population changes), rounds per session, minutes per round, the share of rounds that shipped vs declined vs blocked, how sessions ended, and whether the plateau rule (§4) is firing for the right reason.
   c. Rule quality: is the 0.60 solidify floor, the 20-page floor and its two exceptions, the three-attempt repair limit, the per-template/subject grouping (§1b), the authority order, or the stop rules producing bad outcomes — declines that look derivable, blocked items that a rule could have settled, dips named as "artefacts" without a companion number, exhaustion verdicts that the next session refuted? Quote real examples from the Round log.
   d. Gate health: pairing parser, pairs skipped, any gate whose baseline moved without an explained cause, any verifier that could be passing vacuously, the committed baseline vs the live one. Compare EVERY aggregate field of the committed gate_baseline.json with the live column of the newest FULL gatecheck log (a field the finalise scripts stopped updating shows there as a stale delta — the r494–r498 case), and read each verifier's RESULT logic for a count-vs-baseline test (one that passes on defect 0 alone is vacuous when its builder stops building).
   e. Session mechanics: stalls, compactions per session (they are logged one line each since 22 Sept), commands that ran past the timeout, hook blocks, anything the session had to be prompted for; the Round-log lines' ≈ times against the commit times (three sessions to 25 Sept wrote times up to 80 minutes ahead of the clock); whether §5c, §5d and §6 need tightening.
   f. Intake state (§1f): whether new gold modules have arrived since the last review, whether Round 0d ran for them, whether the ceiling, dashboard, registries, miner and gate baseline were re-based, and whether the pending queue was re-prioritised against the new families.
   g. Decisions owed by Chris: the "Needs Chris" list in LOOP_STATE.md, one line each, oldest first, with how many pages each is holding up; add anything new the sessions raised and strike anything now decided.

4. WRITE AND APPLY. Write the report to LOOP_REVIEW__<YYYY-MM-DD>.md at the folder root, in plain English with the numbers, with a numbered list of the concrete changes (quote the current text and the new text for each; what happens if made, what happens if not). Then APPLY EVERY ONE OF THEM in this session — mirror and commit the amended files FIRST ("Loop review <date> — draft") so no amendment ever exists only as an uncommitted file outside git, then run the adversarial read below, then amend and commit again — LOOP__Autonomous_Rounds.md edited in place (never rewritten from an older copy; add the date to its "Amended:" line), the §7 message and the loop-start SKILL.md kept byte-identical and state-free, the other SKILL.md copies if their text must change, settings.json hooks if a mechanical safeguard is needed, KB_AMALGAMATION_STATUS.md's header and rows, the loop mirror refreshed and proven byte-identical. Verify the amended files with an independent adversarial read (contradictions between sections, a fresh-session dry run of the start message) and fix what it finds. Then in LOOP_STATE.md: record Chris's in-session instructions under "## Decisions from Chris"; refresh the "## Needs Chris" list; append a "## Loop review <date>" entry (what was assessed, what changed, what was left, what the next review must check first); and rewrite the "Next session starts with:" line so the next /loop-start begins with the right Round 1 under the amended rules. Commit in pageforge-site with the message "Loop review <date>". Never push. Finish with the plain-English summary and the two-line push block.
