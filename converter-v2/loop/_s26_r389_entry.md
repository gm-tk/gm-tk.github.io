## 2026-09-19 (round 389, build 260619.60) — THE BUILT FLIPCARD GROUP CLOSES ITS COLUMN: the prose after a built `div.row.flipCardsContainer` opens a new row (`body_region.row_breaks.after_built_widgets`, env `FLIPBREAK_OFF`); the autonomous loop's session 26 Round 3 — the r388 paired row-break census cut by the built widget's own signature (the flipCard group is the ONE built widget whose after-rule the gold breaks: paired 0.82; every other built widget a tie at 0.55); SCOPED regeneration of the 21 affected modules (scoped ship #1 since the r388 full); skeleton 53.455 → 53.470 % (+0.016pp; 29 movers 23 up / 6 down, the dips named), ≥50 1155 → 1157, ≥75 192 → 191 (OSAH301_1_0, named), ≥90 15, RAW 37.719 → 37.725 %; every other gate EXACT

### 1. WHAT CHANGED

**The r51 `flow_blocks` rule keeps every built widget IN the section row and lets the next prose flow on after it — and for the built flipCard group that is wrong.** Claude emitted `p` → `div.row.flipCardsContainer` → `p` inside ONE `col-md-8`; the gold closes the column after the group and opens a fresh `row > col-md-8` for the prose (CEDO501 2.0: `Flip these images to learn more…` → the 3-card group → `Let's unpack these types of income…` in its own row).

**The fix (DATA OVER CODE):** `Emit_Templates.json` `body_region.row_breaks.after_built_widgets` `{enabled, env: "FLIPBREAK_OFF", rules: [{class_match: "\\bflipCardsContainer\\b", templates: ["Standard"], exclude_subjects: ["Leaving to Learn", "NCEA1", "1-10 English"]}]}`; `ContentConverter.#breaksAfterBuilt(html, run)` tests the BUILT html's open-tag class against the rule (plus the module's `module_meta` template_type / subject and an optional per-rule env) and the INLINE widget site — a top-level widget, `!stack.length` — calls `breakRow()` after it. The bundle-owned site (a widget inside an activity box) is untouched: the group never closes the box. OFF = the r388 output on every page (the in-memory probe 2109 / 2109).

### 2. HOW IT WAS FOUND (the r388 instrument, cut finer)

- **`outputs/_s26_r389_divpair.py` → `.out`** — the paired row-break census, direction B (Claude FLOWS after block X; does the gold break?), keyed on the FULL signature of the `div` kind: **`div.flipCardsContainer.row` gold-breaks 23 / flows 5 = 0.82 (26 pages / 20 modules; the next block always a `p`)** — Standard: English 3 / 0, ConnectED 6 / 0, Online Safety 8 / 2 = 0.80, Leaving to Learn 3 / 2 = 0.60 (n = 5); Inquiry 2 / 1 (under the floor). The other `div` kinds are singletons (ratio / TKmodal / shapeHover). The reader's WIDGET kind — every OTHER built widget, collapsed — is a tie at 0.55, so r51 stands for them.
- **`outputs/_s26_r389_flipgold.py` → `.out`** — the gold's own form over every top-level gold flipCardsContainer (187; 146 pages / 83 modules Standard): position in its column `only` 82 / `last` 58 / `middle` 45 / `first` 2 → **LAST-or-only 0.75** (the after-break) but **FIRST-or-only 0.45 = a tie** → the break BEFORE the group is NOT taken; the column class col-md-8 118 / col-md-12 58 → Claude's col-md-8 stays. Per subject after-share: English 0.87, Mathematics 0.93, ConnectED 0.82, Online Safety 0.82, NCEA1 0.71 (n = 7), ANZH 0.33 (n = 6), **Leaving to Learn 0.32 (19 / 28 `middle`)**.
- **KB-first check:** 03A / 05D give the flipCard build (`row.flipCardsContainer > col > flipCard`); nothing on the section row after the group → authority LOOP §1b level 3 / 4. Structure-only → derivable by construction.

### 3. THE PROBE (in memory, one toggle state per process — `outputs/_s26_r389_probe.cjs` / `_s26_r389_probe_run.sh`, 4 shards over all 416 modules)

- **OFF (`FLIPBREAK_OFF=1`): identical 2109 / changed 0 / missing 0** — byte-identical to the r388 disk on every page.
- **ON, first cut (no subject exclusions): 54 pages / 40 modules, scored with the gate's own `match()` BEFORE regenerating: 52 paired, 26 up / 26 down, pp-sum +22.5** — decomposed by subject (`_s26_r389_bysubject.log`): Online Safety 12 / 4 (+15.1), ConnectED 6 / 0 (+10.7), Mathematics 2 / 0 (+2.6), Science 1 / 0 (+1.6), the subject-less TEDC pair 2 / 2 (+0.7), **Leaving to Learn 0 / 13 (−5.3 — its gold keeps prose after the group, the census's 0.32), NCEA1 0 / 3 (−3.6), 1-10 English 3 / 4 (+0.7, a tie; the paired census 3 / 0 on 3 boundaries, under the floor)** → the three excluded by subject (the session-25 lesson: the gate's own scorer decides).
- **ON, shipped: changed 29 pages / 21 modules** (`_s26_r389_changed_modules.txt` / `_s26_r389_affected.txt`): CEDO501 CEDT501 CEDW501 MXDB302 MXFL301 OSAH301 OSAH501 OSAI501 OSBY101 OSBY201 OSBY401 OSBY501 OSGM201 OSGM301 OSGM401 OSOH101 OSOH401 OSSC501 SCCH301 TEDC401 TEDC402 — **29 paired pages, 23 up / 6 down / 0 same, pp-sum +30.7 (mean +1.06pp per changed page)** (`_s26_r389_pagescore.py` → `_s26_r389_onscore.json`). Best: OSBY501_3_0 53.6 → 57.8, CEDO501_2_0 49.4 → 52.9 (crosses ≥50), OSSC501_2_0 60.4 → 63.0, OSAH501_4_0 63.9 → 66.1, CEDW501_3_0 51.4 → 53.6, CEDT501_7_0 55.9 → 57.8, MXFL301_1_0 (crosses ≥50). **The 6 dips are the family's gold-flow minority, named:** OSAH301_1_0 −2.8 (76.2 → 73.4, the ≥75 down-crosser — its gold keeps the paragraph after the group in the same column), OSOH101_3_0 −1.0, OSBY101_1_0 −0.5, OSOH401_1_0 −0.3, TEDC401_4_0 −0.2, TEDC402_2_0 −0.1.

### 4. THE SCOPED REGENERATION + THE GATES (scoped ship #1 since the r388 full — `_ship_ledger.py`: 7 of headroom)

- 21 modules in 4 batches (`_s26_r389_batches.sh` / `_s26_r389_regen_run.sh`, every batch rc 0); `_content_manifest.py fresh --affected` → **0 truly stale: 21 affected regenerated, the untouched 392 byte-identical to the manifest** (`_s26_r389_fresh.log`); the manifest diff = **29 pages / 21 modules changed, 0 added / removed** (`_s26_r389_manifest_diff.log`); **probe ON == regenerated disk on 155 / 155 pages** (`_s26_r389_regen_vs_probe.log`).
- **PRIMARY skeleton (`_s26_r389_sk_final.json`, a fresh full run): SCAFFOLD 53.4546 → 53.4702 % (+0.016pp) / ≥50 1155 → 1157 (CEDO501_2_0, MXFL301_1_0 cross up) / ≥75 192 → 191 (OSAH301_1_0 crosses down — accepted BY NAME, the r338 / r342 precedent: the family's gold-flow minority) / ≥90 15 / RAW 37.719 → 37.725 % @ 1956 pairs, skipped 0; 29 movers (23 up / 6 down, pp-sum +30.7 = the probe's prediction exactly), 0 added / dropped, 0 movers outside the affected set; = 58.2 % of achievable** (ceiling 91.9 %).
- compare_structure exact **11643** / EXTRA **172** / MISSING **625** EXACT; body_compare **42 / 4 / 173 / 218** EXACT; clean **2079 / 2102**, leak **26 / 23** EXACT; tags **9557 / 9557 REAL 0**; every verifier RESULT line identical to r388; entry-parity PASS; index-sync OK; all 15 verifier selftests + the skeleton selftest GREEN (`_s26_r389_selftests.log`); fast-loop baseline, content manifest and feature index refreshed (`_s26_r389_postship.sh`).
- The DIFF MINER re-mined on the r389 corpus: **177 CANDIDATE rows (178 → 177, 0 new — the `body MISSING col-md-8 › WIDGET` row, best group Online Safety lessons c = 0.65, fell below consensus once the OS flipCard groups end their column)**.

### 5. RECORDED

- The 6 gold-flow pages are the family's editorial minority (no discriminator — same widget, same writer shape); Leaving to Learn (gold 0.32 after-break), NCEA1 and 1-10 English (a scorer tie) are excluded by subject; the subject-less TEDC pair (2 / 2, +0.7) stays in.
- Not taken from the census: the break BEFORE the group (gold FIRST-or-only 0.45, a tie; ConnectED 21 / 28 `only` is the one family that isolates it — under the module floor); the col-md-12 width (0.31); the other built widgets (WIDGET 0.55, a tie).
